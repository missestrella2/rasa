rest:

# Ejemplo de canal Rasa (si se utiliza Rasa X / Enterprise):
rasa:
  url: "http://localhost:5002/api"

# Otros canales (Facebook, Slack, SocketIO, etc.) se dejan comentados si no se usan:
# facebook:
#   verify: "<verify>"
#   secret: "<your secret>"
#   page-access-token: "<your page access token>"

# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true
